import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App loads without crashing', (WidgetTester tester) async {
    // This is a placeholder — replace with actual app test when ready
    expect(true, isTrue);
  });
}
